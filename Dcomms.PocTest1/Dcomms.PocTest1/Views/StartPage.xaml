<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Dcomms.PocTest1.Views.StartPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
               
                <StackLayout Orientation="Horizontal">
                    <Label Text="Neighbors to keep:" VerticalOptions="Center" /><!--Text="{Binding DrpTester3.NumberOfNeighborsToKeep, Mode=TwoWay}"-->
                    <Entry WidthRequest="60" x:Name="numberOfNeighbors"  TextChanged="NumberOfNeighbors_TextChanged"/>
                    <Button x:Name="increaseNumberOfNeighbors" Text="increase"  Clicked="IncreaseNumberOfNeighbors_Clicked"/>
                </StackLayout>

                <Button Text="Start" Command="{Binding DrpTester3.InitializeUser}" IsVisible="{Binding DrpTester3.Initialized, Converter={StaticResource OppositeConverter}}" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>